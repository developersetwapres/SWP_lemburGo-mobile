abstract final class ApiConfig {
  static const baseUrl = 'http://kmz-lemburgo.test/api';
  static const connectTimeout = Duration(seconds: 20);
  static const receiveTimeout = Duration(seconds: 30);
}
